import React from 'react'

const Title = (props) => {
    return (
        <div className = "hero">
            <h1 className = "hero-body">Needle Nanny</h1>
        </div>
    )
}
export default Title;